# Typus Models Configuration File
#
# Use the README file as a reference to customize settings.

Post:
  fields:
    default: nome, tel, comentario
    form: nome, tel, comentario
  order_by: 
  relationships: 
  filters: created_at
  search: 
  application: project

